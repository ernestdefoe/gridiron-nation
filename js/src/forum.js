import app from 'flarum/forum/app';

app.initializers.add('ernestdefoe-fbsfb', () => {
  // GridIron Nation theme — JS initializer.
  // Currently a no-op; reserved for future widget bootstrapping
  // (live scores auto-refresh, recruit tracker, etc.)
});
